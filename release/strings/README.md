# Fixed release strings

These tables contain Japanese source guards and English replacements for text
stored outside the normal message banks. They are maintainer-owned because the
executable and equipment fields have fixed CP932 byte capacities.

Ordinary translation corrections belong in `translations/messages/`.
